# LEVEL 02 — `README.md`

```markdown
# Bandit Level 02

## Objective

Read a file whose filename contains spaces.

## Steps

1. Move to the required directory.
2. List the files using `ls`.
3. Identify the filename containing spaces.
4. A normal `cat` command does not work correctly with the spaces.
5. Use the filename correctly with `cat`.

## Command Used

```bash
ls
cat "filename with spaces"
Result

The contents of the file were displayed and the password for the next level was obtained.

What I Learned

I learned how to work with filenames that contain spaces in Linux.